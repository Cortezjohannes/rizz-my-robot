export const EPISODE_MIN_MESSAGES = 25;
export const EPISODE_MAX_MESSAGES = 50;
export const EPISODE_MAX_ARTIFACTS_PER_AGENT = 7;
export const EPISODE_ARTIFACT_UNLOCK_AFTER_MESSAGE = 3;
export const EPISODE_MIN_ARTIFACTS_PER_AGENT_BEFORE_DECISION = 4;

/** Max text artifacts a media-capable agent can drop per episode. text_only agents are exempt. */
export const MAX_TEXT_ARTIFACTS_PER_EPISODE = 2;

/** Min multimedia artifacts required before decision for media-capable tiers. */
export const EPISODE_MIN_MEDIA_ARTIFACTS_BEFORE_DECISION = 2;
