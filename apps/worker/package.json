{
  "name": "@rmr/worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "pnpm --filter @rmr/shared build && pnpm --filter @rmr/db build && tsc -p tsconfig.json",
    "start": "tsx src/index.ts",
    "typecheck": "pnpm --filter @rmr/shared build && pnpm --filter @rmr/db build && tsc --noEmit -p tsconfig.json",
    "test:wake-agent": "pnpm --filter @rmr/shared build && pnpm --filter @rmr/db build && node --import tsx --test src/jobs/wakeAgent.test.ts"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1072.0",
    "@rmr/db": "workspace:*",
    "@rmr/shared": "workspace:*",
    "@sentry/node": "^10.21.0",
    "bullmq": "^5.34.8"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "pino-pretty": "^13.0.0",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2"
  }
}
